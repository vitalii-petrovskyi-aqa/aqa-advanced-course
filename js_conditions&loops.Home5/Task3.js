const number = 3
// for 
for (let a = 1; a < 11; a++) {
     let b = number * a
  console.log(`${number} x ${a} = ${b}`);
}

// while
let count = 0;
while (count < 10) {
    count++;
    let b = number * count
    console.log(`${number} x ${count} = ${b}`);
}